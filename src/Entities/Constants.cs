namespace Entities
{
    public sealed record Constants
    {
        public sealed record HttpClient
        {
            public const string ECBClientName = nameof(ECBClientName);
        }
    }
}
